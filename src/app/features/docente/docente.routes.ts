import { Routes } from '@angular/router';

export const DOCENTE_ROUTES: Routes = [
  {
    path: '', // Base path para /docente
    // loadComponent: () => import('./dashboard/dashboard').then(m => m.DashboardComponent) // Comentado temporalmente hasta que se cree
  }
];
